name: exemplo
description: Exemplo de arquivo pubspec.yaml
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  http: ^0.13.3
  google_maps_flutter: ^2.1.1
  geolocator: ^7.7.0
  firebase_auth: ^3.3.3
  cloud_firestore: ^3.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true